{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PATCHRequestEnvelope",
  "description": "Standard envelope for a partial update request using a field mask.",
  "type": "object",
  "properties": {
    "updateMask": {
      "type": "string",
      "description": "A comma-separated list of field paths (using dot notation) indicating fields to update or clear.",
      "pattern": "^[a-zA-Z0-9_\\.]+(,[a-zA-Z0-9_\\.]+)*$"
    },
    "resource": {
      "type": "object",
      "description": "The resource payload containing values or nulls for fields specified in the updateMask."
    }
  },
  "required": [
    "updateMask",
    "resource"
  ]
}
